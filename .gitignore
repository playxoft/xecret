# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# build output
.next/
out/
build/
dist/
.open-next/
*.tsbuildinfo
next-env.d.ts

# testing
coverage/

# ── secrets: nothing resembling a real credential may ever be committed ──
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
*.pem
*.key
*.enc
secrets.json

# Written by `phase init`. Contains no secrets — only the ids of the Phase.dev
# app and environment this checkout is linked to — but those are one person's
# account, and a fork pointing at somebody else's app is worse than no file at
# all. Every contributor runs `phase init` for their own.
.phase.json

# cloudflare
.wrangler/
.vercel

# go
cli/dist/
cli/xecret
cli/*.test

# editors
.idea/
.vscode/
*.swp

# os
.DS_Store
Thumbs.db

# logs
*-debug.log*
*-error.log*

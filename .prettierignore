node_modules/
.next/
.open-next/
.wrangler/
dist/
coverage/
package-lock.json
cli/
*.md
packages/db/migrations/

# Generated by `npm run cf-typegen --workspace @xecret/web` from wrangler.toml.
# Committed so a fresh clone typechecks without running wrangler, and ignored
# here because formatting it would be undone by the next regeneration.
apps/web/src/cloudflare-env.d.ts

# Per-machine Claude Code settings. Git-ignored globally, so it is somebody's
# local file rather than part of the repository — `npm run verify` should not
# fail on how their editor wrote it.
.claude/settings.local.json

# Generated by `npm run -w @xecret/core import-fixtures:generate`, whose layout
# is JSON.stringify's. Prettier collapses short arrays onto one line and
# JSON.stringify never does, so formatting it would make every regeneration fail
# the format check — and the generator, not the formatter, is the authority on a
# file two test suites compare against byte for byte.
packages/core/src/importer/fixtures/import-fixtures.json

# Node.js + xecret

The complete guide lives at [docs/guides/nodejs.md](../../docs/guides/nodejs.md).

```bash
xecret run -- node server.js
```

Delete `require('dotenv').config()` — `process.env` already has the values.

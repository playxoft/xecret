# Next.js + xecret

The complete guide lives at [docs/guides/nextjs.md](../../docs/guides/nextjs.md).

The whole integration:

```bash
xecret init
xecret run -- next dev
```

No `.env.local`, no loader — `process.env` is populated before Next starts.
